import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { LanguageProvider } from "@/lib/language";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GitHub Value Tracker | AI-Powered Open Source Intelligence",
  description: "Discover unicorn projects before they go mainstream. AI-powered analysis of GitHub repositories with commercial potential.",
  keywords: "GitHub, open source, project analysis, AI, investment, startup, value tracking",
  authors: [{ name: "GitHub Value Tracker" }],
  openGraph: {
    title: "GitHub Value Tracker",
    description: "Discover unicorn projects before they go mainstream",
    type: "website"
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <LanguageProvider>
          {children}
        </LanguageProvider>
      </body>
    </html>
  );
}
